# Profil développement (H2 en mémoire)
spring.datasource.url=jdbc:h2:mem:centre_commercial;MODE=MYSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Flyway désactivé sur H2 en mémoire (schéma recréé)
spring.flyway.enabled=false

jwt.secret=s3CuReR4nd0mK3yOfAtLeast32Chars!42
jwt.expiration=86400000
# Development Profile - MySQL (désactivé, on utilise MySQL maintenant)
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

# H2 Console (désactivé)
# spring.h2.console.enabled=false

# JPA
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.jpa.show-sql=true

